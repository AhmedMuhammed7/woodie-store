const Info = () => (
  <div className="text">
    <p>
      This dining chair is UV mapped so you can use your own texture. The model
      also is in real world scale so you can drop it into any environment and
      with 3 fabric material presets inside 3dMax archive.
    </p>
  </div>
);

export default Info;
